spring:
  application:
    name: java-docker-sample
  datasource:
    url: jdbc:postgresql://localhost:5432/java-docker-sample
    username: ed-wantuil
    password: complex-password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
